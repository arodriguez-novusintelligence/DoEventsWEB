import { AdminStaffTab } from '../../../pages/admin/tabs/AdminStaffTab';

/** Panel usuarios admin — delega en `AdminStaffTab` (UI Lovable + API `@doevents/shared`). */
export const AdminUsersPanel = () => <AdminStaffTab />;

export default AdminUsersPanel;
