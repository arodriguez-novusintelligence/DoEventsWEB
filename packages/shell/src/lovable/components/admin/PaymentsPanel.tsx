import { AdminPaymentsTab } from '../../../pages/admin/tabs/AdminPaymentsTab';

/** Panel pagos admin — delega en `AdminPaymentsTab` (UI Lovable + API real). */
export const PaymentsPanel = () => <AdminPaymentsTab />;

export default PaymentsPanel;
