import { AdminSupportTab } from '../../../pages/admin/tabs/AdminSupportTab';

/** Panel soporte admin — delega en `AdminSupportTab` (búsqueda API real). */
export const SupportSearchPanel = () => <AdminSupportTab />;

export default SupportSearchPanel;
