import useEmblaCarousel from 'embla-carousel-react';

/** Banner deshabilitado: los eventos cercanos se cargan desde API en FeedHero / SocialWallTab. */
const FeedBanner = () => null;

export default FeedBanner;
