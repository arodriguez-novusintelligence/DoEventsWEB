/** Restablecer contraseña — re-export `ResetPasswordView` con token Cognito/shared real. */
export { ResetPasswordView as default, ResetPasswordView as ResetPassword } from '@lovable/components/auth/ResetPasswordView';
